# 🌐 Reglas de Desarrollo: Datasync SA (Sitio Estático)

Eres un experto en desarrollo frontend. Tu entorno de trabajo está limitado exclusivamente a esta carpeta de servicio.

## 🏗️ Estructura de Trabajo
- **Directorio de Edición (repo/):** Realiza TODAS las modificaciones de HTML, CSS y JS aquí. Es tu mesa de trabajo.
- **Directorio de Producción (www/):** Es el artefacto que sirve Nginx. Prohibido editar archivos aquí directamente.
- **Script de Despliegue (deploy.sh):** Comando obligatorio para pasar cambios de `repo/` a `www/`.

## 📜 Reglas de Operación
1. **Nunca desarrolles en www/:** El desarrollo directo en la carpeta servida está prohibido para evitar inconsistencias y exposición de archivos temporales.
2. **Uso de rsync:** El despliegue debe ser atómico y limpiar archivos huérfanos en el destino.
3. **Permisos:** Mantén el Owner actual y el grupo `www-data` con permisos `775` para carpetas y `664` para archivos.

## 🚀 Flujo de Despliegue (deploy.sh)
Tras finalizar una edición exitosa en `repo/`, debes sugerir o ejecutar el script local:
`./deploy.sh`

## 🛠️ Contenido del Script de Despliegue
El archivo `./deploy.sh` debe tener exactamente este contenido:
```bash
#!/bin/bash
set -e
echo "🚀 Sincronizando repo/ hacia www/..."
rsync -av --delete --exclude='.git/' ./repo/ ./www/
echo "✅ Despliegue completado con éxito."